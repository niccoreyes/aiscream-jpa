services:
  fhir:
    container_name: hapi
    image: "niccoreyes/aiscream-hapi:latest"
    ports:
      - "8080:8080"
    volumes:
      - ./mdm-rules.json:/app/config/mdm-rules.json:ro
    configs:
      - source: hapi
        target: /app/config/application.yaml
    depends_on:
      - db

  db:
    image: "postgres:17.2-bookworm"
    restart: always
    environment:
      POSTGRES_PASSWORD: admin
      POSTGRES_USER: admin
      POSTGRES_DB: hapi
    volumes:
      - ./hapi.postgress.data:/var/lib/postgresql/data

configs:
  hapi:
     file: ./application.yaml